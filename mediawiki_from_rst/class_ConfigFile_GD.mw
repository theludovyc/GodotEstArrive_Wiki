__NOTOC__'''Inherits:''' [[Reference GD|Reference]] '''<''' [[Object GD|Object]]Helper class to handle INI-style files.== Description ==This helper class can be used to store [[Variant GD|Variant]] values on the filesystem using INI-style formatting. The stored values are identified by a section and a key:::    [section]    some_key=42    string_example="Hello World!"    a_vector=Vector3( 1, 0, 2 )The stored data can be saved to or parsed from a file, though ConfigFile objects can also be used directly without accessing the filesystem.The following example shows how to parse an INI-style file from the system, read its contents and store new values in it:::    var config = ConfigFile.new()    var err = config.load("user://settings.cfg")    if err == OK: # If not, something went wrong with the file loading        # Look for the display/width pair, and default to 1024 if missing        var screen_width = config.get_value("display", "width", 1024)        # Store a variable if and only if it hasn't been defined yet        if not config.has_section_key("audio", "mute"):            config.set_value("audio", "mute", false)        # Save the changes by overwriting the previous file        config.save("user://settings.cfg")Keep in mind that section and property names can't contain spaces. Anything after a space will be ignored on save and on load.ConfigFiles can also contain manually written comment lines starting with a semicolon ({{Span|;}}). Those lines will be ignored when parsing the file. Note that comments will be lost when saving the ConfigFile. This can still be useful for dedicated server configuration files, which are typically never overwritten without explicit user action.== Methods =={| class="wikitable | void|| [[#erase_section|erase_section]] '''(''' [[String GD|String]] section ''')'''                                                                            |-| void|| [[#erase_section_key|erase_section_key]] '''(''' [[String GD|String]] section, [[String GD|String]] key ''')'''                                   |-| [[PackedStringArray GD|PackedStringArray]]|| [[#get_section_keys|get_section_keys]] '''(''' [[String GD|String]] section ''')''' const                                                                |-| [[PackedStringArray GD|PackedStringArray]]|| [[#get_sections|get_sections]] '''(''' ''')''' const                                                                                                            |-| [[Variant GD|Variant]]|| [[#get_value|get_value]] '''(''' [[String GD|String]] section, [[String GD|String]] key, [[Variant GD|Variant]] default=null ''')''' const |-| bool|| [[#has_section|has_section]] '''(''' [[String GD|String]] section ''')''' const                                                                          |-| bool|| [[#has_section_key|has_section_key]] '''(''' [[String GD|String]] section, [[String GD|String]] key ''')''' const                                 |-| [[@GlobalScope GD#Error|Error]]|| [[#load|load]] '''(''' [[String GD|String]] path ''')'''                                                                                                 |-| [[@GlobalScope GD#Error|Error]]|| [[#load_encrypted|load_encrypted]] '''(''' [[String GD|String]] path, [[PackedByteArray GD|PackedByteArray]] key ''')'''                          |-| [[@GlobalScope GD#Error|Error]]|| [[#load_encrypted_pass|load_encrypted_pass]] '''(''' [[String GD|String]] path, [[String GD|String]] password ''')'''                             |-| [[@GlobalScope GD#Error|Error]]|| [[#parse|parse]] '''(''' [[String GD|String]] data ''')'''                                                                                               |-| [[@GlobalScope GD#Error|Error]]|| [[#save|save]] '''(''' [[String GD|String]] path ''')'''                                                                                                 |-| [[@GlobalScope GD#Error|Error]]|| [[#save_encrypted|save_encrypted]] '''(''' [[String GD|String]] path, [[PackedByteArray GD|PackedByteArray]] key ''')'''                          |-| [[@GlobalScope GD#Error|Error]]|| [[#save_encrypted_pass|save_encrypted_pass]] '''(''' [[String GD|String]] path, [[String GD|String]] password ''')'''                             |-| void|| [[#set_value|set_value]] '''(''' [[String GD|String]] section, [[String GD|String]] key, [[Variant GD|Variant]] value ''')'''              |}== Method Descriptions ===== erase_section ===*  void '''erase_section''' '''(''' [[String GD|String]] section ''')'''Deletes the specified section along with all the key-value pairs inside. Raises an error if the section does not exist.=== erase_section_key ===*  void '''erase_section_key''' '''(''' [[String GD|String]] section, [[String GD|String]] key ''')'''Deletes the specified key in a section. Raises an error if either the section or the key do not exist.=== get_section_keys ===*  [[PackedStringArray GD|PackedStringArray]] '''get_section_keys''' '''(''' [[String GD|String]] section ''')''' constReturns an array of all defined key identifiers in the specified section. Raises an error and returns an empty array if the section does not exist.=== get_sections ===*  [[PackedStringArray GD|PackedStringArray]] '''get_sections ( )''' constReturns an array of all defined section identifiers.=== get_value ===*  [[Variant GD|Variant]] '''get_value''' '''(''' [[String GD|String]] section, [[String GD|String]] key, [[Variant GD|Variant]] default=null ''')''' constReturns the current value for the specified section and key. If either the section or the key do not exist, the method returns the fallback {{Span|default}} value. If {{Span|default}} is not specified or set to {{Span|null}}, an error is also raised.=== has_section ===*  bool '''has_section''' '''(''' [[String GD|String]] section ''')''' constReturns {{Span|true}} if the specified section exists.=== has_section_key ===*  bool '''has_section_key''' '''(''' [[String GD|String]] section, [[String GD|String]] key ''')''' constReturns {{Span|true}} if the specified section-key pair exists.=== load ===*  [[@GlobalScope GD#Error|Error]] '''load''' '''(''' [[String GD|String]] path ''')'''Loads the config file specified as a parameter. The file's contents are parsed and loaded in the {{Span|ConfigFile}} object which the method was called on.Returns one of the [[@GlobalScope GD#Error|Error]] code constants ({{Span|OK}} on success).=== load_encrypted ===*  [[@GlobalScope GD#Error|Error]] '''load_encrypted''' '''(''' [[String GD|String]] path, [[PackedByteArray GD|PackedByteArray]] key ''')'''Loads the encrypted config file specified as a parameter, using the provided {{Span|key}} to decrypt it. The file's contents are parsed and loaded in the {{Span|ConfigFile}} object which the method was called on.Returns one of the [[@GlobalScope GD#Error|Error]] code constants ({{Span|OK}} on success).=== load_encrypted_pass ===*  [[@GlobalScope GD#Error|Error]] '''load_encrypted_pass''' '''(''' [[String GD|String]] path, [[String GD|String]] password ''')'''Loads the encrypted config file specified as a parameter, using the provided {{Span|password}} to decrypt it. The file's contents are parsed and loaded in the {{Span|ConfigFile}} object which the method was called on.Returns one of the [[@GlobalScope GD#Error|Error]] code constants ({{Span|OK}} on success).=== parse ===*  [[@GlobalScope GD#Error|Error]] '''parse''' '''(''' [[String GD|String]] data ''')'''Parses the the passed string as the contents of a config file. The string is parsed and loaded in the ConfigFile object which the method was called on.Returns one of the [[@GlobalScope GD#Error|Error]] code constants ({{Span|OK}} on success).=== save ===*  [[@GlobalScope GD#Error|Error]] '''save''' '''(''' [[String GD|String]] path ''')'''Saves the contents of the {{Span|ConfigFile}} object to the file specified as a parameter. The output file uses an INI-style structure.Returns one of the [[@GlobalScope GD#Error|Error]] code constants ({{Span|OK}} on success).=== save_encrypted ===*  [[@GlobalScope GD#Error|Error]] '''save_encrypted''' '''(''' [[String GD|String]] path, [[PackedByteArray GD|PackedByteArray]] key ''')'''Saves the contents of the {{Span|ConfigFile}} object to the AES-256 encrypted file specified as a parameter, using the provided {{Span|key}} to encrypt it. The output file uses an INI-style structure.Returns one of the [[@GlobalScope GD#Error|Error]] code constants ({{Span|OK}} on success).=== save_encrypted_pass ===*  [[@GlobalScope GD#Error|Error]] '''save_encrypted_pass''' '''(''' [[String GD|String]] path, [[String GD|String]] password ''')'''Saves the contents of the {{Span|ConfigFile}} object to the AES-256 encrypted file specified as a parameter, using the provided {{Span|password}} to encrypt it. The output file uses an INI-style structure.Returns one of the [[@GlobalScope GD#Error|Error]] code constants ({{Span|OK}} on success).=== set_value ===*  void '''set_value''' '''(''' [[String GD|String]] section, [[String GD|String]] key, [[Variant GD|Variant]] value ''')'''Assigns a value to the specified key of the specified section. If either the section or the key do not exist, they are created. Passing a {{Span|null}} value deletes the specified key if it exists, and deletes the section if it ends up empty once the key has been removed.