__NOTOC__Float built-in type.== Description ==Float built-in type.== Methods =={| class="wikitable | float|| [[#float|float]] '''(''' bool from ''')'''     |-| float|| [[#float|float]] '''(''' int from ''')'''       |-| float|| [[#float|float]] '''(''' [[String GD|String]] from ''')''' |}== Method Descriptions ===== float ===*  float '''float''' '''(''' bool from ''')'''Cast a bool value to a floating-point value, {{Span|float(true)}} will be equal to 1.0 and {{Span|float(false)}} will be equal to 0.0.*  float '''float''' '''(''' int from ''')'''Cast an int value to a floating-point value, {{Span|float(1)}} will be equal to 1.0.*  float '''float''' '''(''' [[String GD|String]] from ''')'''Cast a [[String GD|String]] value to a floating-point value. This method accepts float value strings like {{Span|"1.23"}} and exponential notation strings for its parameter so calling {{Span|float("1e3")}} will return 1000.0 and calling {{Span|float("1e-3")}} will return 0.001. Calling this method with an invalid float string will return 0. This method stops parsing at the first invalid character and will return the parsed result so far, so calling {{Span|float("1a3")}} will return 1 while calling {{Span|float("1e3a2")}} will return 1000.0.