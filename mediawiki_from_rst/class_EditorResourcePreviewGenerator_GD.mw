__NOTOC__'''Inherits:''' [[Reference GD|Reference]] '''<''' [[Object GD|Object]]Custom generator of previews.== Description ==Custom code to generate previews. Please check {{Span|file_dialog/thumbnail_size}} in [[EditorSettings GD|EditorSettings]] to find out the right size to do previews at.== Methods =={| class="wikitable | bool|| [[#can_generate_small_preview|can_generate_small_preview]] '''(''' ''')''' virtual                                                      |-| [[Texture2D GD|Texture2D]]|| [[#generate|generate]] '''(''' [[Resource GD|Resource]] from, [[Vector2 GD|Vector2]] size ''')''' virtual                 |-| [[Texture2D GD|Texture2D]]|| [[#generate_from_path|generate_from_path]] '''(''' [[String GD|String]] path, [[Vector2 GD|Vector2]] size ''')''' virtual |-| bool|| [[#generate_small_preview_automatically|generate_small_preview_automatically]] '''(''' ''')''' virtual                                  |-| bool|| [[#handles|handles]] '''(''' [[String GD|String]] type ''')''' virtual                                                           |}== Method Descriptions ===== can_generate_small_preview ===*  bool '''can_generate_small_preview ( )''' virtualIf this function returns {{Span|true}}, the generator will call [[#generate|generate]] or [[#generate_from_path|generate_from_path]] for small previews as well.By default, it returns {{Span|false}}.=== generate ===*  [[Texture2D GD|Texture2D]] '''generate''' '''(''' [[Resource GD|Resource]] from, [[Vector2 GD|Vector2]] size ''')''' virtualGenerate a preview from a given resource with the specified size. This must always be implemented.Returning an empty texture is an OK way to fail and let another generator take care.Care must be taken because this function is always called from a thread (not the main thread).=== generate_from_path ===*  [[Texture2D GD|Texture2D]] '''generate_from_path''' '''(''' [[String GD|String]] path, [[Vector2 GD|Vector2]] size ''')''' virtualGenerate a preview directly from a path with the specified size. Implementing this is optional, as default code will load and call [[#generate|generate]].Returning an empty texture is an OK way to fail and let another generator take care.Care must be taken because this function is always called from a thread (not the main thread).=== generate_small_preview_automatically ===*  bool '''generate_small_preview_automatically ( )''' virtualIf this function returns {{Span|true}}, the generator will automatically generate the small previews from the normal preview texture generated by the methods [[#generate|generate]] or [[#generate_from_path|generate_from_path]].By default, it returns {{Span|false}}.=== handles ===*  bool '''handles''' '''(''' [[String GD|String]] type ''')''' virtualReturns {{Span|true}} if your generator supports the resource of type {{Span|type}}.