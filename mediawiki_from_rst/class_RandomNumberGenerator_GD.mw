__NOTOC__'''Inherits:''' [[Reference GD|Reference]] '''<''' [[Object GD|Object]]A class for generating pseudo-random numbers.== Description ==RandomNumberGenerator is a class for generating pseudo-random numbers. It currently uses `PCG32 <http://www.pcg-random.org/>`_.'''Note:''' The underlying algorithm is an implementation detail. As a result, it should not be depended upon for reproducible random streams across Godot versions.To generate a random float number (within a given range) based on a time-dependant seed:::    var rng = RandomNumberGenerator.new()    func _ready():        rng.randomize()        var my_random_number = rng.randf_range(-10.0, 10.0)== Properties =={| class="wikitable | int|| [[#seed|seed]]|| {{Span|-6398989897141750821}} |}== Methods =={| class="wikitable | float|| [[#randf|randf]] '''(''' ''')'''                                                                               |-| float|| [[#randf_range|randf_range]] '''(''' float from, float to ''')'''      |-| float|| [[#randfn|randfn]] '''(''' float mean=0.0, float deviation=1.0 ''')''' |-| int|| [[#randi|randi]] '''(''' ''')'''                                                                               |-| int|| [[#randi_range|randi_range]] '''(''' int from, int to ''')'''              |-| void|| [[#randomize|randomize]] '''(''' ''')'''                                                                       |}== Property Descriptions ===== seed ===*  int '''seed'''{| class="wikitable | ''Default''|| {{Span|-6398989897141750821}} |-| ''Setter''|| set_seed(value)          |-| ''Getter''|| get_seed()               |}The seed used by the random number generator. A given seed will give a reproducible sequence of pseudo-random numbers.'''Note:''' The RNG does not have an avalanche effect, and can output similar random streams given similar seeds. Consider using a hash function to improve your seed quality if they're sourced externally.== Method Descriptions ===== randf ===*  float '''randf ( )'''Generates a pseudo-random float between {{Span|0.0}} and {{Span|1.0}} (inclusive).=== randf_range ===*  float '''randf_range''' '''(''' float from, float to ''')'''Generates a pseudo-random float between {{Span|from}} and {{Span|to}} (inclusive).=== randfn ===*  float '''randfn''' '''(''' float mean=0.0, float deviation=1.0 ''')'''Generates a `normally-distributed <https://en.wikipedia.org/wiki/Normal_distribution>`_ pseudo-random number, using Box-Muller transform with the specified {{Span|mean}} and a standard {{Span|deviation}}. This is also called Gaussian distribution.=== randi ===*  int '''randi ( )'''Generates a pseudo-random 32-bit unsigned integer between {{Span|0}} and {{Span|4294967295}} (inclusive).=== randi_range ===*  int '''randi_range''' '''(''' int from, int to ''')'''Generates a pseudo-random 32-bit signed integer between {{Span|from}} and {{Span|to}} (inclusive).=== randomize ===*  void '''randomize ( )'''Setups a time-based seed to generator.